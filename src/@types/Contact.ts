export interface Contact {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  branchId: string;
  whenToSell: string;
  description: string;
  confirmTerms: boolean;
}
